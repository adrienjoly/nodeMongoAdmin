var titi = require('./titi');

console.log(titi.coco);



  var dbUriReg = /(?:mongodb:\/\/)?([^:]+:[^@]+@)?([\w\.\-\_]+)(:\d+)?\/(.+)/;
  var userReg = /([^:]+):([^@]+)@/;
  var uriMatch = dbUri.match(dbUriReg);
  var userMatch, user, password, host, port, dbName;
  if (uriMatch) {
    if (uriMatch[1]) {
      userMatch = uriMatch[1].match(userReg);
      user = userMatch[1];
      password = userMatch[2];
    }
    host = uriMatch[2];
    port = uriMatch[3] ? parseInt(uriMatch[3].substr(1)) : 27017;
    dbName = uriMatch[4];
  } else {
    host = 'localhost';
    port = 27017;
    dbName = dbUri;
  }